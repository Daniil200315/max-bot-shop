services:
  shop:
    build: .
    restart: unless-stopped
    environment:
      - TZ=Asia/Novokuznetsk
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
